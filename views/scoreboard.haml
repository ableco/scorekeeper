!!!
%html
  %head
    %title
      Disrupto
    %link{ :rel => "stylesheet", :href => "/css/bootstrap.css" }
    %link{ :rel => "stylesheet", :href => "/css/bootstrap-responsive.css" }
    %link{ :rel => "stylesheet", :href => "/css/memberly.css" }
    %link{ :rel => "stylesheet", :href => "/css/application.css" }

  %body{ :onload => "//javascript:setTimeout('location.reload(true);',5000);" }
    .container
      .row
        .container.logo.col-md-4
          %img{ :src => "/images/logo-dark.png" }
      .row
        .container.extraordinary-people.col-md-8
          %table
            %thead
              %th(colspan=2)
                Extraordinary People
                %br
                (in order of extraordinariness)
            %tbody
              - @rankings.each do |name, score|
                %tr
                  %td.name
                    = name
                  %td
                    = score

    %script{ :src => "//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js" }
    %script{ :src => "/js/bootstrap.min.js" }